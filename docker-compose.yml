services:
  app:
    build: .
    container_name: voucher-system
    restart: unless-stopped
    ports:
      - "3001:3001"
    volumes:
      - ./data:/app/data
    env_file:
      - ./backend/.env
    environment:
      - DB_PATH=/app/data
      - NODE_ENV=production
